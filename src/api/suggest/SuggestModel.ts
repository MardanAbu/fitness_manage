export type SuggestParm = {
    currentPage:number,
    pageSize:number,
    title:string,
    total:number
}

export type SuggestType = {
    type:string,
    id:string,
    title:string,
    content:string
}