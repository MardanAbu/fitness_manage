export type ListParm = {
    name:string,
    currentPage:number,
    pageSize:number,
    total:number
}

export type EquipmentType = {
    id:string,
    name:string,
    details:string,
    numTotal:number,
    type:string
}